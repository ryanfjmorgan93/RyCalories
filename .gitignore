*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks

# Personal-app signing key: committed on purpose so every build (local or CI) installs over the last.
!app/keystore/*.jks
