---
description: "Bootstrap pointer that installs the external repo-scan skill from a pinned, reviewable commit"
---

Invoke the `repo-scan` skill via the Skill tool, then follow its instructions.

Arguments from the user, if any: $ARGUMENTS
