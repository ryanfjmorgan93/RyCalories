---
description: "Create beautiful, accessible user interfaces with shadcn/ui components (built on Radix UI + Tailwind), Tailwind CSS utility-first…"
---

Invoke the `ui-styling` skill via the Skill tool, then follow its instructions.

Arguments from the user, if any: $ARGUMENTS
